/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://www.umangliving.com", // your live domain
    generateRobotsTxt: true, // also create robots.txt
    sitemapSize: 5000, // splits sitemap if too many URLs
  };
  